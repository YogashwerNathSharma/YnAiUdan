{
  "name": "ynaiudan",
  "private": true,
  "version": "0.1.0",
  "description": "YnAiUdan - production-grade multimodal AI agent platform",
  "packageManager": "pnpm@10.12.1",
  "scripts": {
    "dev": "pnpm --parallel --filter @ynaiudan/web --filter @ynaiudan/api dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "db:generate": "pnpm --filter @ynaiudan/api exec prisma generate",
    "db:format": "pnpm --filter @ynaiudan/api exec prisma format",
    "db:migrate": "pnpm --filter @ynaiudan/api exec prisma migrate dev"
  }
}